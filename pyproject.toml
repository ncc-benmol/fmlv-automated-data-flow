[project]
name = "fmlv-automated-data-flow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.1",
]

[project.scripts]
fmlv-automated-data-flow = "fmlv_automated_data_flow:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
